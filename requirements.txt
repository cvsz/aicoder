anthropic>=0.75.0
# 0.75.0+ needed for client.beta.agents / .environments / .sessions
# (Claude Managed Agents — see claude_agents_sdk.ManagedAgentsClient) and
# the streaming client used elsewhere. Most other modules in this project
# talk to the Messages API directly over urllib and don't need the SDK at
# all, so an older pin would still "work" for those — but --agent-managed-run
# needs 0.75.0+ specifically.
python-dotenv>=1.0.0

# Vision / multimodal — no extra deps (base64 + urllib)
# Batch API — no extra deps
# Streaming — no extra deps
# Files API — no extra deps

# Optional: for local code execution tool
# (code_exec runs in Anthropic's sandbox; no local deps needed)

# Optional — multimedia subsystem (v1.3)
# gtts>=2.5.0
# pyttsx3>=2.90
# moviepy>=1.0.3
# music21>=9.1.0
